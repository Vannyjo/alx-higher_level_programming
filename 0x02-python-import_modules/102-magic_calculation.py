#!/usr/bin/python3

def magic_calculation(a, b):
    """Match bytecode provided"""
    from magic_calculation_102 import add, sub

    if i < j:
        k = add(i, j)
        for f in range(4, 6):
            k = add(k, f)
        return (k)

    else:
        return (sub(i, j))
